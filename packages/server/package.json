{
  "name": "server",
  "version": "0.0.0",
  "author": "0xYami",
  "license": "MIT",
  "private": true,
  "main": "build/app.js",
  "typings": "build/app.d.ts",
  "files": [
    "build",
    "src"
  ],
  "scripts": {
    "start": "pnpm build && node -r 'dotenv/config' ./build/app.js",
    "dev": "nodemon --watch ./src --ext ts --exec pnpm start",
    "build": "tsup",
    "typecheck": "tsc -p ./tsconfig.json --noEmit"
  },
  "dependencies": {
    "alchemy-sdk": "^2.6.2",
    "fastify": "^4.15.0"
  },
  "devDependencies": {
    "@onix/types": "workspace:*",
    "@types/node": "^18.15.10",
    "dotenv": "^16.0.3",
    "nodemon": "^2.0.22",
    "tslib": "^2.5.0",
    "tsup": "^6.7.0",
    "typescript": "^5.0.2"
  },
  "engines": {
    "node": ">=18"
  },
  "type": "module"
}
